<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <style>
    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-size: large;
    }


    .container{
        width: 100%;
        height: 100vh;
        position: relative;
    }

    .box{
        width: 800px;
        /* height: 500px; */
        position:absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%);
    }


    .table{
        width: 600px;
    }

    .isPromoted-{
        color: yellow;
    }
    .isPromoted-true{
        color: green;
    }
    .isPromoted-false{
        color: red;
    }
    </style>
</head>
<body>
    <div class="container">
    <div class="box">
    <h1>Profiles</h1>
    <table border="1" class="table">
        <thead class="tableHead">
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>City</th>
                <th>Profession</th>
                <th>Promote/Demoted</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
                <% users.forEach((user,i)=>{%>
                <tr class="isPromoted-<%- user.isPromoted %>">
                    <td><%= user.name %></td>
                    <td><%= user.email %></td>
                    <td><%= user.age %></td>
                    <td><%= user.city %></td>
                    <td><%= user.profession %></td>
                    <td>
                    <!-- <% if (user.isPromoted){ %>
                        <form method="POST" action="/user/<%= user._id %>?_method=PUT">
                            <button id="edit-<%= user.name %>" type="submit"></button>
                        </form> 
                    <% } else { %>
                        <form method="POST" action="/user/<%= user._id %>?_method=PUT">
                            <button id="edit-<%= user.name %>" type="submit">Demote</button>
                        </form>
                    <% } %> -->

                    <form method="POST" action="/user/<%= user._id %>?_method=PUT">
                    <button id="edit-<%= user._id %>" type="submit">
                        <% if (user.isPromoted) { %>
                                    Demote
                                <% } else { %>
                                    Promote
                                <%} %> 
                            </button>
                        </form>
                    </td>
                    <td>
                        <form method="POST" action="/user/<%= user._id %>?_method=DELETE">
                            <button id="delete-<%= user._id %>">Delete</button>
                        </form>
                    </td>
                </tr>
                

                <%})%>
        </tbody>
    </table>
    <br>
    <a href="form"><button>Add</button></a>
    </div>
    </div>
</body>
</html>